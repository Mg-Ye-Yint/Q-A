import { Box, Typography } from "@mui/material"

const Home = () => {
    return (
        <Box>
            <Typography sx={{color: "#1C1C1C", fontSize: "32", fontWeight: "bold"}}>
                Start With
            </Typography>
        </Box>
    )
}

export default Home